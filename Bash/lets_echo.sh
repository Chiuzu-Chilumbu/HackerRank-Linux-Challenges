#!/bin/bash -x

# This is a shebang. It tells the system this script should be executed with /bin/bash.
# The -x option is used to debug the script. It prints the command before executing it.
# Echo command is used to print to the terminal
echo 'Hello'